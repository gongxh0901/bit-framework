# pnpm 配置文件

# 注册源
registry=https://registry.npmjs.org/

# 使用严格的对等依赖
strict-peer-dependencies=false

# node_modules 结构 (可选配置)
# shamefully-hoist=true  # 将所有依赖提升到根目录（类似 npm）
# node-linker=hoisted    # 使用 hoisted 结构（推荐）
# node-linker=isolated   # 使用默认的隔离结构（最节省空间）

# 全局存储路径（注释掉则使用系统默认的 ~/.pnpm-store）
# store-dir=~/.pnpm-store

# 模块目录（默认 node_modules）
# modules-dir=node_modules
